server:
  port: 8081
spring:
  cloud:
    config:
      server:
        bootstrap: false
        git:
          default-label: main
          uri: https://github.com/Alex-mk2/lab2TingesoMicroservicios
          search-paths: config-data
  application:
    name: backend-config-service
